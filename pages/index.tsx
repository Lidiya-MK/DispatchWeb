import React from "react";

export default function IndexPage(): JSX.Element {
  // The actual SPA is mounted from pages/_app (client-only).
  // This page intentionally renders nothing — the client app handles routing.
  return <div />;
}
